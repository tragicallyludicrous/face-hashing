# Generated meshes (derive from personal photos) — top-level and nested under viewer/models/
viewer/models/*.glb
viewer/models/**/*.glb

# macOS Google Drive bookmark (machine-specific alias; legacy Colab handoff)
Drive Folder

# Claude Code local settings
.claude/settings.local.json

# Local Mac-native pipeline — generated (model checkouts, run I/O, scratch).
# Keep local/*.py and local/README.md tracked.
local/MICA/
local/smirk/
local/in/
local/out*/
# scraped celebrity faces + review sheets/grids (copyrighted; tools/fetch_faces.py)
local/fetch*/
# bridge conditioning maps (depth/mask/normal) — derive from personal/copyrighted photos
local/bridge_test/
local/arcface_tmp/
local/temp.py

# Generated inspection figures/JSON (derive from personal photos -> keep local)
tools/figures/

# arcface hash corpora/galleries (per-face embeddings -> keep local). The fitted whitening
# *basis* (aggregate stats only, from synthetic faces) is fine to commit if you want.
*gallery*.npz
*gallery*.npy
*corpus*.npz
*corpus*.npy

# FLAME shape basis exported for viewer/sliders.html — registration-gated, non-commercial license
viewer/flame/

# Junk / Python
.DS_Store
__pycache__/
*.pyc
.venv/
*.zip
Presentation/