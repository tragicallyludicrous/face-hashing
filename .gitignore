# Outputs (kept local / synced via the Google Drive alias, not committed)
outputs/*
!outputs/.gitkeep
viewer/models/*.glb

# Personal input photos — live in the Google Drive alias for Colab handoff
inputs/

# FLAME 2020 model weights — registration-gated, non-commercial license, ~153MB
colab/FLAME2020/

# macOS Google Drive bookmark (machine-specific alias)
Drive Folder

# Claude Code local settings
.claude/settings.local.json

# Local Mac-native MICA runner — generated (the MICA clone, run I/O, scratch).
# Keep local/patch_mica_for_mac.py and local/README.md tracked.
local/MICA/
local/smirk/
local/in/
local/out*/
local/arcface_tmp/
local/temp.py

# .glb outputs nested under viewer/models/ (top-level *.glb already covered above)
viewer/models/**/*.glb

# Generated inspection figures/JSON (derive from personal photos -> keep local)
tools/figures/

# FLAME shape basis exported for viewer/sliders.html — registration-gated, non-commercial license
viewer/flame/

# Junk / Python
.DS_Store
__pycache__/
*.pyc
.venv/
